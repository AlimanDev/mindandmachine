# Generated by aa on 2018-05-20 04:59

from django.db import migrations, models


def add_cashbox_type(apps, schema_editor):
    # We can't import the Person model directly as it may be a newer
    # version than this migration expects. We use the historical version.
    WorkerDayCashboxDetails = apps.get_model('db', 'WorkerDayCashboxDetails')
    Cashbox = apps.get_model('db', 'Cashbox')
    CashboxType = apps.get_model('db', 'CashboxType')

    for ct in CashboxType.objects.all():
        WorkerDayCashboxDetails.objects.filter(on_cashbox__type_id=ct.id).update(cashbox_type_id=ct.id)


class Migration(migrations.Migration):

    dependencies = [
        ('db', '0017_auto_20180520_0456'),
    ]

    operations = [
        migrations.RunPython(add_cashbox_type)
    ]