run script in django shell

echo 'import src.db.debug.fill_data' | python3 manage.py shell

OR

python3 manage.py shell
and type 'import src.db.debug.fill_data'

===

nginx config for redirect to backend ololo.com/foo/bar -> localhost/bar

location /foo/ {
    proxy_pass http://localhost:3200/; # note the trailing slash!
}

===

add jquery via browser console

var s = document.createElement('script');
s.type = 'text/javascript';
s.src='https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js';
document.body.appendChild(s);

===

#!/bin/bash
sudo su postgres -c "dropdb qos_v1 && createdb qos_v1" &&
rm -r src/db/migrations/; ./manage.py makemigrations db &&
./manage.py migrate &&
./manage.py shell -c "import src.db.debug.fill_data as m; m.load_data()" &&
./manage.py shell -c "import src.db.debug.create_superuser as m; m.create('admin', 'q@q.com', '1')"
